# Copy to .env for local dev. For the live site, set the same two values in
# GitHub: Settings -> Secrets and variables -> Actions
#   Variables: VITE_GCAL_ID    Secrets: VITE_GCAL_KEY
VITE_GCAL_ID=your-calendar-id@group.calendar.google.com
VITE_GCAL_KEY=your-browser-api-key
